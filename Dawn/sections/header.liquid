<link rel="stylesheet" href="{{ 'component-list-menu.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-search.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-menu-drawer.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-cart-notification.css' | asset_url }}" media="print" onload="this.media='all'">

<style>
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 3rem;
  }

  .header__heading-link {
    text-decoration: none;
    color: inherit;
  }

  .header__heading-logo {
    height: 30px;
  }

  .header__nav {
    display: flex;
    gap: 2rem;
  }

  .header__nav-link {
    text-decoration: none;
    color: inherit;
  }

  .header__buy-button {
    background-color: #1a1a1a;
    color: #fff;
    padding: 0.5rem 1rem;
    border-radius: 5px;
    text-decoration: none;
  }
</style>

<header class="header">
  <a href="{{ routes.root_url }}" class="header__heading-link">
    <img src="https://purrfectclean-beta.vercel.app/logo.svg" alt="PurrfectClean" class="header__heading-logo">
  </a>

  <nav class="header__nav">
    <a href="#features" class="header__nav-link">Features</a>
    <a href="#how-it-works" class="header__nav-link">How It Works</a>
    <a href="#reviews" class="header__nav-link">Reviews</a>
  </nav>

  <a href="{{ routes.cart_url }}" class="header__buy-button">Buy Now</a>
</header>
